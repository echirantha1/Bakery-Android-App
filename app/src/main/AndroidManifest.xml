<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test" >
        <activity android:name=".AdminRejectedOrders"></activity>
        <activity android:name=".AdminConfirmedOrders" />
        <activity android:name=".AdminManageOrders" />
        <activity android:name=".MemberCart" />
        <activity android:name=".MemberOrder" />
        <activity android:name=".MemberMain" />
        <activity android:name=".ItemAll" />
        <activity android:name=".AdminAddItems" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".MainSignup" />
        <activity
            android:name=".MainLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>